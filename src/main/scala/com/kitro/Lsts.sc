//import com.kitro.collections.ZList

List(1,2,3).foreach(print(_))

//ZList(1,2,3).forEach(x => println(x))
//ZList(1,2,3).sum


List(1,2,3).sum
List("q","we").reduce((x,y) => x.concat(y))

List(1,6).head